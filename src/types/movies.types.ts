export interface movieType {
  title: string;
  release_date: Date;
  rating: number;
  language: string;
  url: string;
  actors: Array<string>;
  directos: Array<string>;
  geners: Array<string>;
}
