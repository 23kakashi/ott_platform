import request from "supertest";
import sinon from "sinon";
import app from "../../app";

describe.skip("login api /auth/login", () => {
  afterEach(() => {
    sinon.restore();
  });
  it("", () => {
    //Arrange

    //Act

    //Assert
    expect(true).toBeTruthy();
  });
});
